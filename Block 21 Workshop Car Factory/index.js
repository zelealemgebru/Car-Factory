class car {
  contructore(make, model, year) {
         getDescription.make = make;
         getDescription.model = model;
         getDescription.year = yaer;                        
              }
getDescription() {
      return `This car has ${getDescription.make} make, ${getDescription.model} model, and is ${getDescription.year} year.`;
    }
}
class ElectricCar extends car {
              contructore(make, model, year, range) {
                           super(make, model, year)
                            getDescription.range = range;
}
getDescription() {
return `${super.getDesc()}It has a ${getDescription.range} mileage.`;}
}


 getDesc() 
              return `This car has ${getDesc.make} make, ${getDesc.model} model, ${getDesc.year} year and is ${getDesc.range} range.`;
         
           
              ElectricCar = getDescElectricCar("Tesla", "Model S", 2019, 300);
              description = electricCar.getDescription();
           

   class ElectricCar {
              contractore(make, model, year, range){
                 getDescriptionc.make = "Tesla"
                 getDescription.model = "Model S"
                 getDescription.year = 2019
                 getDescription.range = 300
                 }
              }

{
console.log();System.out.print(Car);
console.log();System.out.print(ElectricCar);
}